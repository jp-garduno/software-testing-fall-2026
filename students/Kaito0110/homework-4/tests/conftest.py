"""Pytest configuration for homework-4."""

import os
import sys

# Add the homework-4 directory to sys.path so that `src.banking` can be imported
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))
